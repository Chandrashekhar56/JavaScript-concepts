//================================================================================================================================================================//

const sort={
  username:1,
  email:1,
  contact:1,
  id:1,
};
 const temp = Object.keys(sort).reduce((acc,key)=>{
      if(sort[key] === -1){
        acc=`${acc ? `${acc},`:""}${key}:desc`;
      }else{
        acc=`${acc ? `${acc},`:""}${key}:asc`;
      }
      return acc;
    },'')
    
console.log(temp);

//================================================================================================================================================================//

const sort = {
  username: 1,
  email: 1,
  contact: 1,
  id: 1,
};

const temp = Object.keys(sort)
  .map(key => `${key}:${sort[key] === -1 ? 'desc' : 'asc'}`)
  .join(',');

console.log(temp);

//================================================================================================================================================================//

const sort = {
  username: 1,
  email: 1,
  contact: 1,
  id: 1,
};

let temp = '';

Object.keys(sort).forEach(key => {
  if (sort[key] === -1) {
    temp += `${temp ? ',' : ''}${key}:desc`;
  } else {
    temp += `${temp ? ',' : ''}${key}:asc`;
  }
});

console.log(temp);

//================================================================================================================================================================//

